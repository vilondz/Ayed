#include <iostream>
#include <vector>

struct equipo {
    std::string nombre;
    std::vector<std::string> jugadores;
    std::vector<std::string> entrenador;
    int partidos_ganados;
    int partidos_perdidos;
    int partidos_empatados;
    int puntos;
   
};
struct Clasificacion {
    std::vector<int> numero_de_equipos[22]
    clasificacion equipo;
    std::vector<std::string> clasificacion[];
    for (int i; i < numero_de_equipos){
        equipo[i].nombre == clasificacion.nombre;
    }
};
void clasificacion_equipo(int n){
    std::cout 
}

int main() {
    struct equipo equipo[22];
    Clasificacion(equipo[1]);   
    return 0;
}

